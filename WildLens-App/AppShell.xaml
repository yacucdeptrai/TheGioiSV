<?xml version="1.0" encoding="utf-8" ?>
<Shell
    x:Class="WildLensApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:WildLensApp.Pages">

    <TabBar>
        <ShellContent Title="Camera" ContentTemplate="{DataTemplate pages:CameraPage}" Route="Camera"/>
        <ShellContent Title="History" ContentTemplate="{DataTemplate pages:HistoryPage}" Route="History"/>
    </TabBar>
</Shell>