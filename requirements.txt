
# Unified Python dependencies for the whole project (Wildlens-Web Backend + WildLens-Model)
#
# Notes:
# - If you have an NVIDIA GPU and proper CUDA drivers, prefer onnxruntime-gpu. For CPU-only
#   environments, replace it with plain onnxruntime.
# - ultralytics may install a CPU-only torch by default. If you want CUDA-enabled torch, run
#   the helper in WildLens-Model after creating your venv to install a matching CUDA wheel.
#
# Backend (FastAPI) & common libs
fastapi[all]
numpy>=2.3.4
opencv-python-headless>=4.9.0.80

# Inference/model stack
ultralytics==8.3.225
roboflow>=1.1.25
onnx>=1.16.0
onnxruntime-gpu>=1.18.0
PyYAML>=6.0.1
onnxslim>=0.1.71
uvicorn